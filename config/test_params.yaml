data_folder: "/home/saakee/Research/deep-contact-estimator/Data/NumpyFiles/"
model_load_path: "/home/saakee/Research/deep-contact-estimator/results/bipedal_contact_best_val_leg_acc.pt"

# data params
window_size: 10
shuffle: True  # Must match training config for consistent splits
random_seed: 42  # Must match training config for consistent splits
train_ratio: 0.7  # Must match training config
val_ratio: 0.15  # Must match training config

# network params
batch_size: 30

save_to_mat: False
mat_save_path: "/home/saakee/Research/deep-contact-estimator/results/bipedal_test_results.mat"

# python3 src/test.py --config_name config/test_params.yaml