
csv_folder: "/home/saakee/Research/deep-contact-estimator/Data/CSVFiles/"
save_path: "/home/saakee/Research/deep-contact-estimator/Data/NumpyFiles/"

# mode: "train": split data into train/val/test
#       "inference": create numpy for one seq without splitting
mode: "train"

# Train/validation/test split ratios
train_ratio: 0.7
val_ratio: 0.15
# Remaining is test ratio (0.15)

# Notes:
# - The script will process all CSV files in csv_folder
# - Boundary files are saved to prevent window bleeding across different sequences
# - For inference mode, each CSV file is converted to a separate numpy file


# python3 utils/csv2numpy.py --config_name config/csv2numpy_config.yaml
